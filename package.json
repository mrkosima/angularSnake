{
  "name": "angularSnake",
  "version": "0.0.0",
  "description": "Snake game in angularJS",
  "repository": "https://github.com/mrkosima/angularSnake",
  "license": "MIT",
  "dependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1"
  },
  "scripts": {
    "postinstall": "./node_modules/bower/bin/bower install",
    "prestart": "npm install",
    "start": "http-server -c-1 -d false",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}